# Menu Import

This folder contains the seed JSON files for importing the initial menu.

## Usage
1. Copy `menu.seed.example.json` to `menu.seed.json`
2. Populate `menu.seed.json` with your actual menu data.
3. Login to the admin dashboard.
4. Go to the "استيراد المنيو" (Import Menu) section.
5. Click Import.
